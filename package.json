{
  "name": "saturday-cinematic-sandbox",
  "private": true,
  "description": "Development tooling for the Saturday Racing cinematic replay sandbox. Nothing here ships: production loads js/flat.js directly.",
  "scripts": {
    "lint": "eslint --max-warnings 0 js/flat.js tests tools",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0"
  }
}
